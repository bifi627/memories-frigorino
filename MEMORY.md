- [Optimistic create hooks must reconcile temp id](project_optimistic_tempid_reconcile.md) — `useCreateXItem` must swap the `Date.now()` temp id for the real server id in onSuccess (mirror useCreateListItem); else edit-after-add PUTs to a temp id that overflows `{itemId:int}` → SPA fallback → opaque 500. Fixed in recipe + inventory create hooks.
- [List-item slices must fire classification trigger](project_listitem_slice_classification_contract.md) — a slice that creates list items via `list.AddItem` must also call `IProductClassificationTrigger.OnProductReferenced` (no central enforcement), or promote-to-inventory silently breaks; run extraction only when quantity isn't already supplied. Bit CopyRecipeToList (PR #130).
- [500/IT-failure diagnosability](project_it_diagnosability_500s.md) — IT failures now dump server Warning+ logs (incl. the exception) into `dotnet test` output; non-Dev 500s return ProblemDetails+traceId; unmatched `/api/*` returns 404 not the SPA shell. Read the dump first on a red IT.
- [Railway grid hides timestamptz columns](reference_railway_grid_timestamptz_blank.md) — Railway Postgres data-grid renders timestamptz cols as blank (looks NULL); detail panel/query shows real values. Check `WHERE col < '2000-01-01'` to catch genuine MinValue rows.
- [Reqnroll FQN filter can silently skip a scenario](reference_reqnroll_filter_silent_skip.md) — `--filter "FullyQualifiedName~X"` builds the FQN from the sanitized Feature/Scenario *title*, NEVER the `.feature` file name; filtering on the file name matches zero (or the wrong tests) and can report green. Filter on title words, confirm `gesamt: N`.
- [New i18n namespace needs three files](reference_i18n_namespace_two_files.md) — a brand-new top-level i18n namespace must be added to en + de translation.json AND registered in src/types/i18next.d.ts (hand-maintained typed keys), or tsc rejects every t("newNs.*"). Existing-namespace keys need only the JSON.
- [Reqnroll matches steps keyword-sensitively](reference_reqnroll_keyword_sensitive.md) — this repo's Reqnroll requires the binding keyword (Given/When/Then) to match the step; reuse a setup/action step across keywords by double-decorating `[Given]`+`[When]` (precedent: ExpiryCalendarApiSteps). `And` inherits the preceding keyword.
- [EF prunes IS NULL on required-mapped columns](reference_ef_isnull_pruned_on_required_column.md) — LINQ `x == null` → `WHERE FALSE` when the model maps the column required, even if the DB column is nullable; `EF.Property<string?>` doesn't help. Use raw SQL; verify with `.ToQueryString()`. Bit the fractional-index backfill guard (PR #94).
- [Dependency pinning preference](feedback_dependency_pinning.md) — NuGet exact (`10.0.7`), npm caret-minor (`^1.2.3`); never `~`, `*`, `x.*`. Lockfile is the npm reproducibility guarantee.
- [NuGet lockfile refresh needs --force-evaluate](feedback_nuget_lockfile_force_evaluate.md) — after add/remove of a NuGet dep, plain/`--force` restore won't update packages.lock.json graph; use `dotnet restore --force-evaluate` then grep-verify across all locks.
- [Preserve UX on persistence-lift](feedback_preserve_ux_on_persistence_lift.md) — "move state to DB" tasks change storage/sharing, NOT UX; read the real impl, preserve every affordance, surface UX changes as optional questions not baked-in.
- [PWA push-only service worker](project_pwa_push_only_sw.md) — stale-page-on-deploy fixed by dropping Workbox precaching for a push-only SW (PR #78→stage); user wary of SWs + no offline need. Don't reintroduce precaching without a deliberate offline requirement.
- [SendMessage / agent teams enablement](project_agent_teams_sendmessage.md) — subagent-resume needs `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` as an OS env var (setx + restart), NOT settings.json; known CC bug. Now enabled & verified.
- [Escalate on env blockers](feedback_escalate_on_env_blockers.md) — file lock / daemon down / stuck process → ask the user immediately, don't grind; never escalate an unsubstantiated security alarm.
- [Object URLs + StrictMode](feedback_object_url_strictmode.md) — app runs in StrictMode; create+revoke blob object URLs in ONE paired useEffect (never useMemo); cache the Blob in TanStack Query, not the URL. Caught by manual browser verify in rich-list-items #2.
- [Composer keyboard focus](project_composer_keyboard_focus.md) — keepKeyboardOpen (container mousedown preventDefault, exclude inputs); attach menu MUST be Popper not Menu (Modal aria-hides page → mobile keyboard collapses, no Menu prop fixes it); edit-mode autofocus via rAF. PR #76→stage.
- [Flaky undo-toast integration test](project_flaky_undo_toast_it.md) — sonner 5s auto-dismiss race (click swallowed mid-dismiss); ToastSteps hover-to-pin mitigation recurred, so both undo scenarios (Lists + Inventories) are now `@ignore`'d on stage. ToastSteps.cs kept.
- [C# brace style](feedback_csharp_braces.md) — always use block style {} even for single-line conditions and namespaces
- [Visible task list for plan execution](feedback_visible_task_list_for_plans.md) — for multi-task plan runs (subagent-driven/executing-plans), keep a live TaskCreate/TaskUpdate list in chat so the user sees progress
- [Group plan tasks into phases](feedback_group_plan_tasks_into_phases.md) — for complex features with >9 plan tasks, group into logical phases (Backend/Frontend/Finals) and execute iteratively with a review at each phase boundary; keep task numbers stable
- [IT serves ClientApp/build](project_it_serves_clientapp_build.md) — integration harness serves the SPA from ClientApp/build (not live source); run `npm run build` after React edits or new testids won't appear; build/ is gitignored
- [MUI inputs in Playwright IT](feedback_it_mui_select_playwright.md) — MUI Select testid is on FormControl not the combobox (click role=combobox); expose values via slotProps.htmlInput testids; wait out the GET-seeds-input useEffect before filling
- [if/else over ternary for business logic](feedback_ifelse_over_ternary.md) — expand multi-condition `?:` into if/else; ternary only for quick simple comparisons
- [Name compound conditions](feedback_name_compound_conditions.md) — extract multi-clause boolean checks into a meaningfully-named variable before branching; widen callee to nullable if narrowing is lost
- [Flat DB schema, no entity inheritance](feedback_flat_db_schema.md) — model variants as one flat table + enum/type column + nullable columns; avoid EF TPH/TPT (bad past experience)
- [Clean domain separation](feedback_clean_domain_separation.md) — split distinct concepts into distinct fields/flows over overloading one field with mode-dependent meaning ("dirty invariants"); e.g. media caption ≠ text-item text field
- [Jobs decoupled from dashboard plugins](feedback_jobs_decoupled_from_dashboard_plugins.md) — jobs/services log via Microsoft ILogger only, never Hangfire.Console/PerformContext; bridge to dashboard UI in composition root
- [Railway hosting](project_railway_hosting.md) — Frigorino deploys to Railway (PaaS); standard Docker, listens on default 8080, no shell access in distroless images
- [Railway VITE_ build args](project_railway_vite_build_args.md) — VITE_* service vars only reach the SPA bundle if also declared as ARG+ENV in Dockerfile's build_frontend stage; set in every env (stage AND prod). Missing ARG = silent no-op in deploy (e.g. VITE_FCM_VAPID_KEY → no push prompt)
- [No synthetic uptime checks](project_no_synthetic_uptime_checks.md) — Railway free tier sleeps on idle; any periodic ping (Grafana Synthetics, Healthchecks.io, Hangfire heartbeat push) defeats it. Use passive signals (Faro errors, in-process Hangfire metrics) instead.
- [Cron batches send synchronously](project_cron_batch_sync_send.md) — externally-triggered batch jobs (expiry scan) do real work in-request, not via the in-memory Channels queue: the queue has no durability guarantee (lost on restart/crash) and a cron+ledger loss is unrecoverable. Queue is fine for request-triggered best-effort work. (Railway sleeps after ~10 min idle, not instantly post-200.)
- [Prefer Bash over PowerShell](feedback_prefer_bash.md) — Bash tool for all shell commands; PS needs per-exec approval AND Bitdefender blocks Claude Code's PS `-EncodedCommand` analyzer (confirmed false positive)
- [Playwright assertions](feedback_playwright_assertions.md) — use retrying `Expect(...).ToBeVisibleAsync()` / `.Not.ToBeVisibleAsync()`, never snapshot `IsVisibleAsync` for assertions
- [Test assertions: no translated text](feedback_test_assertions_no_translated_text.md) — UI uses `t()`; Reqnroll/Playwright steps assert on testids or `data-*` attributes, never translated text content
- [Use package.json scripts for frontend tooling](feedback_npm_scripts.md) — `npm run lint`/`build`/`fix`/`api`, never bypass with raw `npx eslint`/`tsc`/`prettier`
- [Prettier in verification + CI](feedback_prettier_in_verification.md) — frontend verify = lint + tsc + prettier; local `npm run prettier` (write), CI `prettier:check` guard in ci.yml
- [API client regeneration workflow](feedback_api_regen_workflow.md) — single `npm run api` from `ClientApp/`; build-time MSBuild target emits `openapi.json`, no backend boot/DB needed
- [Remove dead code when found](feedback_remove_dead_code.md) — delete unreferenced files/symbols as part of the current change, not later; verify with grep first, don't leave TODOs
- [Delete tracking items when done](feedback_remove_tracking_items_when_done.md) — remove IDEAS.md/TECH_DEBT.md/bug entries as the finishing step once their work ships; delete only the completed cycle for multi-cycle items
- [Keep Dockerfile in sync; build it to verify](feedback_dockerfile_csproj_sync.md) — update Application/Dockerfile when projects are added/removed, AND always `docker build` at the end of bigger features to catch drift before Railway does
- [Ask user to start Docker daemon](feedback_docker_daemon_check.md) — if Testcontainers / `docker build` errors with daemon-unreachable, prompt the user to start Docker Desktop instead of skipping
- [Exploratory pacing](feedback_exploratory_pacing.md) — when user is comparing options, answer the immediate question and stop; don't auto-generate files or chain steps ahead of their pick
- [Branch workflow: stage is client UAT](project_branch_workflow.md) — stage→main FF promotion; stage hosts a real client, never push test/probe commits there
- [Avoid unnecessary EF migrations](feedback_avoid_unnecessary_migrations.md) — match aggregate constants to existing DB column widths; don't add `HasMaxLength` just for sibling-aggregate symmetry
- [Verify with full tests + Docker build](feedback_verify_with_full_tests_and_docker.md) — after major changes run `dotnet test` on the full SLN AND `docker build`; unit tests alone don't catch pipeline/SPA/Dockerfile regressions
- [Run sln tests for full coverage](feedback_verify_with_integration_tests.md) — `dotnet test Application/Frigorino.sln` covers Test + IT in one run; don't parallelize verify steps that share state (another `dotnet test` ↔ Testcontainers port, or `npm run build` ↔ ClientApp/build/)
- [Lookup docs over DLLs](feedback_lookup_docs_over_dlls.md) — for NuGet/npm API surface questions, order is context7 MCP > WebFetch (repo README/source) > DLL inspection. Never grep/strings/ildasm as a first move.
- [Context7: use MCP, not npx](feedback_context7_use_mcp.md) — call `mcp__context7__resolve-library-id` / `query-docs` directly; never shell out to `npx ctx7@latest` (MCP token is already wired)
- [Never read user-secrets](feedback_dont_read_user_secrets.md) — don't try `dotnet user-secrets list` or open `secrets.json`; override via env vars / launch profiles instead (env > secrets in ASP.NET precedence)
- [Windows kill by port, not TaskStop](feedback_windows_kill_by_port.md) — `TaskStop` on a bash wrapper leaves child node processes (vite/npm) holding the port; use `Get-NetTCPConnection -LocalPort N | Stop-Process -Force` or `taskkill //F //T //PID`
- [Superpowers skills location](reference_superpowers_skills_location.md) — on-disk path to the 14 superpowers SKILL.md sources (claude-plugins-official cache, ver dir e.g. 5.1.0); Read them directly, don't use Skill tool just to inspect.
- [Keep CLAUDE.md terse](feedback_claudemd_terse.md) — plant info with little overhead; one tight paragraph + file pointers, not multi-section walkthroughs. CLAUDE.md is loaded every session.
- [CLAUDE.md maintenance](feedback_claudemd_maintenance.md) — keep it synced via incremental audit-and-update (verify claims, minimal diff), never delete-and-reinit; reinit wipes institutional knowledge.
- [Vendor-agnostic by default](feedback_vendor_agnostic_by_default.md) — for external services (AI/OCR/push/etc.), define `IXxx` interface, keep config/DI/settings vendor-neutral, treat vendor selection as its own task
- [Match artifact format to project](feedback_match_artifact_format_to_project.md) — when capturing an idea, default to project's existing format (IDEAS.md / TECH_DEBT.md), not a full multi-section spec with code blocks
- [No Co-Authored-By trailers](feedback_no_coauthored_by.md) — never add Co-Authored-By or "Generated with Claude" lines to commits; `includeCoAuthoredBy: false` set in settings
- [Scope verification by confidence](feedback_scope_verification_by_confidence.md) — high-confidence tasks get cheap checks (filtered tests / tsc-lint / self-review); full sln test + docker only as a final gate; skip worktree parallelism
- [Dev-up wait](feedback_devup_wait.md) — wait for the stack with one mechanism (script completion + optional single curl probe); no ScheduleWakeup outside /loop
- [Dev-env lifecycle](feedback_dev_env_lifecycle.md) — you OWN dev-up/dev-down; tear down on demand (esp. when a running backend's locked bin/Debug DLLs block `dotnet test`); prefer ephemeral up/down over long-running
- [Dev-up data is disposable](project_devup_data_disposable.md) — local dev-up env data is agent-seeded throwaway, not real client data; mutate freely when verifying UI
- [Worktrees from stage](feedback_worktree_from_stage.md) — always create worktrees off `stage` (git worktree add ... stage + EnterWorktree path=), not main/HEAD; user rejected global baseRef:head
- [Worktree LSP noise](feedback_worktree_lsp_noise.md) — worktrees aren't in the IDE LSP workspace → phantom CS0246 diagnostics every edit; for repeat edit+verify plan execution work in the main checkout on the feature branch instead. Worktree-path diagnostics are noise; main-path ones are real.
- [No recursive subagents](feedback_no_recursive_subagents.md) — user dislikes nested agents; keep a flat orchestrator→workers tree. Reviewers use `subagent_type: Explore` (no Agent tool); implementers (general-purpose) get a firm "don't spawn subagents" clause in-prompt.
- [DB tests use Testcontainers](feedback_db_tests_testcontainers.md) — never SQLite/EF-InMemory for DB-behavior tests (they diverge from real Postgres); write DB-touching tests in Frigorino.IntegrationTests (Reqnroll + Postgres Testcontainers). Frigorino.Test = pure unit/aggregate/slice logic only.
- [npm ci for installs](feedback_npm_ci_for_installs.md) — clean installs (fresh worktree/CI) use `npm ci`; `npm install` only when adding/updating a dependency
- [Re-test skills after REFACTOR](feedback_retest_after_skill_refactor.md) — building/editing discipline skills via writing-skills TDD: re-run a pressure test after REFACTOR edits before claiming validated; ≥2 scenarios incl. boundary case
- [Exit code not tail pipe](feedback_verify_exit_code.md) — piping a test/build cmd (esp. run_in_background) reports the LAST stage's code; capture `${PIPESTATUS[0]}` or read pass/fail lines, don't trust "exit code 0"
- [Verbatim plan needs runtime verify](feedback_verbatim_plan_verify.md) — verbatim-from-plan code passing tsc+lint+verbatim-match ≠ correct; plan-baked DOM/runtime bugs survive static checks; manual browser verify is the net
- [Playwright MCP: re-query MUI inputs](feedback_playwright_mcp_mui_ids.md) — driving the SPA via MCP, target inputs by placeholder/role/testid and re-query before typing; MUI regenerates `:r:` ids on panel mount/unmount
- [Use opus for final holistic review](feedback_opus_for_final_holistic_review.md) — in subagent-driven plan execution, end-of-phase holistic review must run on opus (sonnet missed an EF sentinel bug); per-task reviews can stay sonnet.
- [MUI X DatePicker in Playwright IT](reference_mui_x_datepicker_playwright.md) — value lives in a hidden aria-hidden input; put testid on a wrapper, click+Keyboard.Type in locale order, pin IT browser Locale=en-US for deterministic mask.
- [npm registry release-age block](reference_npm_registry_release_age.md) — this env rejects too-new versions on install; use a broad caret to resolve newest allowed, then pin caret-minor of the resolved version.
- [Never override min-release-age](feedback_never_override_min_release_age.md) — `--min-release-age=0` is never allowed (not even throwaway/testing); pick the newest version that already clears the 7-day quarantine. Stricter than deps-bump's old "CVE only" text.
